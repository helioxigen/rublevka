import { generateMedia } from 'styled-media-query';

// FIXME remove when will be more media
// eslint-disable-next-line
export const media = generateMedia({
  sm: '576px',
  md: '768px',
  lg: '992px',
  xl: '1200px',
});
