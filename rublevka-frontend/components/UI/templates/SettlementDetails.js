import styled from 'styled-components';

export default styled.article`
    background: white;

    padding: 48px 0 0;

    section {
        margin: 0 auto;

        &:not(:first-child) {
            margin-top: 48px;
        }
    }
`;
